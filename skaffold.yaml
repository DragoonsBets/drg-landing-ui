apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/dragoonsbets/drg-landing:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/dragoonsbets/drg-landing:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: drg-landing
        chartPath: charts/drg-landing
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/dragoonsbets/drg-landing'
          image.tag: '{{.DIGEST_HEX}}'
